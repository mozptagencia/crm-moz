# ============================================================
# Moz CRM · Configuração NGINX
# Ficheiro: /etc/nginx/sites-available/crm.moz.pt
# ============================================================

server {
    listen 80;
    server_name crm.moz.pt;

    # Redirecionar HTTP → HTTPS (activar após Certbot)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass         http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# ── Bloco HTTPS (gerado automaticamente pelo Certbot) ─────────
# server {
#     listen 443 ssl;
#     server_name crm.moz.pt;
#
#     ssl_certificate     /etc/letsencrypt/live/crm.moz.pt/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/crm.moz.pt/privkey.pem;
#     include             /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         proxy_pass         http://localhost:3000;
#         proxy_http_version 1.1;
#         proxy_set_header   Upgrade $http_upgrade;
#         proxy_set_header   Connection 'upgrade';
#         proxy_set_header   Host $host;
#         proxy_set_header   X-Real-IP $remote_addr;
#         proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
